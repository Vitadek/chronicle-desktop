# Rust / Tauri build output
src-tauri/target/
src-tauri/gen/

# Assembled server payload for deb/AppImage bundling (generated by prep-payload.sh)
src-tauri/resources/chronicle/

# Flatpak build output
build-dir/
.flatpak-builder/
repo/
*.flatpak
*.flatpak.asc
SHA256SUMS

# Node (only the seeded plugin's dev symlink, if created)
node_modules
